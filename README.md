# Gotdata

Gotdata is a minimal connection with SQL Server database, using T-SQL to read and write data.

## Examples

Run examples code:

```bash
make run_examples
```

## Tests

Make all tests:

```bash
make tests
```